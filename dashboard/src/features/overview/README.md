# Overview

Owns the dashboard hero, totals, ticker, day strip, and their pure trend projections. It may import shared UI, analytics, API contracts, and providers; it does not import another feature.
