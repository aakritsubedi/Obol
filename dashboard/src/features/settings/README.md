# Settings

Owns settings modal controls, budget editing, history-window, refresh-interval and refresh-floor tuning, and theme selection. Persistence is delegated to the app providers and shared API client.
